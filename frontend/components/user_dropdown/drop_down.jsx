

function DropDownMenu({currentUser, }) {

}


const mapStateToProps = state => {
    return {

    }
}

const mapDispatchToProps = dispatch => {
    return {

    };
};



// Username, profile picture next to it
// gray line indicating a break
// channel and a couple of dead links that will lead to silly things (make it up)
// another break
// settings (this will go to a profile page)
// dark theme (when clicked says 'coming soon' or something?)
// another break
// logout button